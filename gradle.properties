org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official
# Project-level properties
# Default version code used for local builds. CI will override VERSION_CODE with GitHub run number.
VERSION_CODE=100
# Do not hardcode VERSION_NAME here; prefer xmanifest.md via build.gradle.kts. Keep as fallback if needed.
VERSION_NAME=1.00
